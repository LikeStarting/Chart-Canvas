class ScaleY {
  createScale (scaleConfig, top, bottom) {
    switch (scaleConfig.type) {
      case 'log':
        break
      case 'linear':
        break
      case 'pow':
        break
      default:
        break
    }
  }
}

const scaleY = new ScaleY()
export default scaleY
